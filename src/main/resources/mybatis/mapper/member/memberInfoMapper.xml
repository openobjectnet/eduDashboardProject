<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberInfoMapper">

<insert id="insert" parameterType="com.eduDashboardProject.member.vo.MemberInfoVO">

	INSERT INTO member_info(mno,birth,email,job)
	VALUES(#{mno},#{birth},#{email},#{job})

</insert>

<select id="read" resultType="com.eduDashboardProject.member.vo.MemberInfoVO">

	SELECT m.id,m.name,i.birth,i.email,i.job
	from member m
	INNER JOIN member_info i ON m.mno = i.mno 

</select>

<update id="update">
	UPDATE member_info
	
	SET 
		birth = #{birth},
		email = #{email},
		job = #{job}
	
	WHERE mno = #{mno}


</update>












</mapper>